"""Trajectory service — stub.

Trajectory computation is currently handled inside
``app.agents.wound_agent._compute_trajectory``.
This module is reserved for future expansion (e.g. statistical smoothing,
trend alerts over longer windows).
"""
