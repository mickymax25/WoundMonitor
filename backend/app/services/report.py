"""Report service — stub.

Report generation is currently handled inside
``app.models.medgemma.MedGemmaWrapper.generate_report``.
This module is reserved for future expansion (e.g. PDF export,
multi-format reports, template overrides).
"""
