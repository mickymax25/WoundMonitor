"""Analysis service — stub.

The core analysis logic is implemented in ``app.agents.wound_agent.WoundAgent``.
This module is reserved for future refactoring if additional service-layer
abstractions become necessary (e.g. async task queuing, caching).
"""
